on: push
jobs: 
  testing-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          pwd
          ls ./.github/actions/hello
          cd ./
          ls -la
      - uses: ./.github/actions/hello
        id: hello
        with:
          who-to-greet: "World"
      - run: |
          ls -la
          echo "Time: ${{ steps.hello.outputs.time }}"
